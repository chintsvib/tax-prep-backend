# Database: SQLite for local dev, Postgres for production (e.g. Neon)
DATABASE_URL=sqlite:///./tax_prep.db
# DATABASE_URL=postgresql://user:password@host/dbname

# JWT signing key — generate a random string for production
SECRET_KEY=change-me-in-production

# OpenAI — needed for PDF extraction and AI optimization summaries
OPENAI_API_KEY=sk-...

# CORS — comma-separated frontend origins
ALLOWED_ORIGINS=http://localhost:5173
